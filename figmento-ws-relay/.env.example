# Figmento WebSocket Relay — Environment Variables
#
# PORT - Server port. Railway sets this automatically.
#   Fallback chain: PORT → FIGMENTO_RELAY_PORT → 3055
PORT=3055

# FIGMENTO_RELAY_PORT - Legacy local dev port (used if PORT is not set)
# FIGMENTO_RELAY_PORT=3055

# MAX_PAYLOAD - Maximum WebSocket message size in bytes (default: 10 MB)
MAX_PAYLOAD=10485760

# ALLOWED_ORIGINS - Comma-separated list of allowed WebSocket origins.
#   Use '*' to allow all origins (development only).
#   For production, restrict to Figma plugin origins:
# ALLOWED_ORIGINS=https://www.figma.com,https://figma.com
ALLOWED_ORIGINS=*

# RATE_LIMIT_MAX_CONNECTIONS - Maximum concurrent WebSocket connections per IP (default: 10)
RATE_LIMIT_MAX_CONNECTIONS=10
