{
  "name": "figmento-plugin",
  "version": "2.0.0",
  "description": "Figmento Figma plugin — WebSocket-driven design executor",
  "scripts": {
    "build": "node build.js",
    "build:prod": "node build.js --prod",
    "watch": "node build.js --watch",
    "dev": "node build.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}
